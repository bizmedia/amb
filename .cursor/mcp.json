{
  "mcpServers": {
    "message-bus": {
      "command": "node",
      "args": ["${workspaceFolder}/packages/mcp-server/dist/cli.js"],
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/.cursor/mcp.env",
      "env": {
        "AMB_MCP_BOOTSTRAP_LOG": "1"
      }
    }
  }
}
