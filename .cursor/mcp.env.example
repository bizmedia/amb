# Скопируйте в mcp.env и подставьте значения (файл mcp.env в git не коммитится).
# Cursor подхватывает его через envFile в .cursor/mcp.json

MESSAGE_BUS_URL=http://localhost:3334
MESSAGE_BUS_PROJECT_ID=
MESSAGE_BUS_ACCESS_TOKEN=
